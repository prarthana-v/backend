const cartItem = require("../services/cartService");
const UserService = require("../services/userService");

async function updateCartItem(userId, cartItemId, cartItemData) {
  try {
  } catch (error) {
    throw new Error("");
  }
}
